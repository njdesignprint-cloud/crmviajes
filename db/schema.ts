import { integer, real, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const agencies = sqliteTable("agencies", { id: integer("id").primaryKey({ autoIncrement: true }), name: text("name").notNull(), status: text("status").notNull().default("trial"), trialEndsAt: text("trial_ends_at"), plan: text("plan").notNull().default("trial"), phone: text("phone").notNull().default(""), stripeCustomerId: text("stripe_customer_id"), stripeSubscriptionId: text("stripe_subscription_id"), createdAt: text("created_at").notNull(), updatedAt: text("updated_at") });
export const agencyMembers = sqliteTable("agency_members", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull(), email: text("email").notNull().unique(), displayName: text("display_name").notNull().default(""), role: text("role").notNull().default("agent"), active: integer("active", { mode: "boolean" }).notNull().default(true), createdAt: text("created_at").notNull() });
export const clients = sqliteTable("clients", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull().default(1), name: text("name").notNull(), email: text("email").notNull().default(""), phone: text("phone").notNull().default(""), destination: text("destination").notNull().default("Por definir"), status: text("status").notNull().default("Nuevo"), createdAt: text("created_at").notNull(), archivedAt: text("archived_at") });
export const trips = sqliteTable("trips", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull().default(1), clientId: integer("client_id").notNull(), destination: text("destination").notNull(), startDate: text("start_date").notNull(), endDate: text("end_date").notNull().default(""), total: real("total").notNull().default(0), status: text("status").notNull().default("Cotización") });
export const payments = sqliteTable("payments", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull().default(1), tripId: integer("trip_id").notNull(), amount: real("amount").notNull(), dueDate: text("due_date").notNull(), paidAt: text("paid_at"), method: text("method").notNull().default("Pendiente"), note: text("note").notNull().default("") });
export const tasks = sqliteTable("tasks", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull().default(1), clientId: integer("client_id"), title: text("title").notNull(), dueDate: text("due_date").notNull(), priority: text("priority").notNull().default("Normal"), completed: integer("completed", { mode: "boolean" }).notNull().default(false) });
export const quotes = sqliteTable("quotes", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull().default(1), clientId: integer("client_id").notNull(), destination: text("destination").notNull(), travelers: integer("travelers").notNull().default(1), subtotal: real("subtotal").notNull().default(0), taxes: real("taxes").notNull().default(0), total: real("total").notNull().default(0), status: text("status").notNull().default("Borrador"), validUntil: text("valid_until").notNull(), createdAt: text("created_at").notNull(), notes: text("notes").notNull().default(""), shareTokenHash: text("share_token_hash"), viewedAt: text("viewed_at"), acceptedAt: text("accepted_at"), acceptedName: text("accepted_name"), convertedTripId: integer("converted_trip_id") });
export const activities = sqliteTable("activities", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull().default(1), clientId: integer("client_id").notNull(), kind: text("kind").notNull().default("Nota"), detail: text("detail").notNull(), createdAt: text("created_at").notNull() });
export const auditLogs = sqliteTable("audit_logs", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull(), actorEmail: text("actor_email").notNull(), action: text("action").notNull(), entityType: text("entity_type").notNull(), entityId: integer("entity_id"), createdAt: text("created_at").notNull() });
export const travelers = sqliteTable("travelers", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull(), clientId: integer("client_id").notNull(), firstName: text("first_name").notNull(), lastName: text("last_name").notNull(), birthDate: text("birth_date").notNull().default(""), nationality: text("nationality").notNull().default(""), notes: text("notes").notNull().default("") });
export const suppliers = sqliteTable("suppliers", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull(), name: text("name").notNull(), category: text("category").notNull(), contactName: text("contact_name").notNull().default(""), email: text("email").notNull().default(""), phone: text("phone").notNull().default(""), notes: text("notes").notNull().default("") });
export const bookings = sqliteTable("bookings", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull(), tripId: integer("trip_id").notNull(), supplierId: integer("supplier_id").notNull(), serviceType: text("service_type").notNull(), confirmation: text("confirmation").notNull().default(""), saleAmount: real("sale_amount").notNull().default(0), costAmount: real("cost_amount").notNull().default(0), commissionAmount: real("commission_amount").notNull().default(0), commissionDueDate: text("commission_due_date").notNull().default(""), commissionReceivedAt: text("commission_received_at"), status: text("status").notNull().default("Confirmada") });
export const quoteItems = sqliteTable("quote_items", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull(), quoteId: integer("quote_id").notNull(), category: text("category").notNull(), description: text("description").notNull(), quantity: integer("quantity").notNull().default(1), unitPrice: real("unit_price").notNull().default(0), total: real("total").notNull().default(0), sortOrder: integer("sort_order").notNull().default(0) });
export const automationEvents = sqliteTable("automation_events", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull(), eventKey: text("event_key").notNull().unique(), createdAt: text("created_at").notNull() });
export const emailCodes = sqliteTable("email_codes", { id: integer("id").primaryKey({ autoIncrement: true }), email: text("email").notNull(), codeHash: text("code_hash").notNull(), purpose: text("purpose").notNull().default("signup"), payloadJson: text("payload_json").notNull().default("{}"), attempts: integer("attempts").notNull().default(0), expiresAt: text("expires_at").notNull(), consumedAt: text("consumed_at"), createdAt: text("created_at").notNull() });
export const userSessions = sqliteTable("user_sessions", { id: integer("id").primaryKey({ autoIncrement: true }), memberId: integer("member_id").notNull(), tokenHash: text("token_hash").notNull().unique(), expiresAt: text("expires_at").notNull(), lastSeenAt: text("last_seen_at").notNull(), createdAt: text("created_at").notNull() });
export const billingEvents = sqliteTable("billing_events", { id: integer("id").primaryKey({ autoIncrement: true }), providerEventId: text("provider_event_id").notNull().unique(), eventType: text("event_type").notNull(), agencyId: integer("agency_id"), payloadJson: text("payload_json").notNull().default("{}"), createdAt: text("created_at").notNull() });
export const salesNotes = sqliteTable("sales_notes", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull(), authorEmail: text("author_email").notNull(), note: text("note").notNull(), createdAt: text("created_at").notNull() });
export const platformBilling = sqliteTable("platform_billing", { id: integer("id").primaryKey({ autoIncrement: true }), agencyId: integer("agency_id").notNull().unique(), monthlyPrice: real("monthly_price").notNull().default(0), billingStatus: text("billing_status").notNull().default("not_configured"), nextBillingAt: text("next_billing_at"), updatedAt: text("updated_at").notNull() });
