import { integer, real, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const clients = sqliteTable("clients", { id: integer("id").primaryKey({ autoIncrement: true }), name: text("name").notNull(), email: text("email").notNull().default(""), phone: text("phone").notNull().default(""), destination: text("destination").notNull().default("Por definir"), status: text("status").notNull().default("Nuevo"), createdAt: text("created_at").notNull() });
export const trips = sqliteTable("trips", { id: integer("id").primaryKey({ autoIncrement: true }), clientId: integer("client_id").notNull(), destination: text("destination").notNull(), startDate: text("start_date").notNull(), endDate: text("end_date").notNull().default(""), total: real("total").notNull().default(0), status: text("status").notNull().default("Cotización") });
export const payments = sqliteTable("payments", { id: integer("id").primaryKey({ autoIncrement: true }), tripId: integer("trip_id").notNull(), amount: real("amount").notNull(), dueDate: text("due_date").notNull(), paidAt: text("paid_at"), method: text("method").notNull().default("Pendiente"), note: text("note").notNull().default("") });
export const tasks = sqliteTable("tasks", { id: integer("id").primaryKey({ autoIncrement: true }), clientId: integer("client_id"), title: text("title").notNull(), dueDate: text("due_date").notNull(), priority: text("priority").notNull().default("Normal"), completed: integer("completed", { mode: "boolean" }).notNull().default(false) });
